name: Test React App
on: push
jobs:
  runs-on: ubuntu-latest
  steps:
    - name: Get the repository code
      uses: actions/checkout@v5
    - name: Install dependencies
      run: npm ci
    - name: Run tests
      run: npm run test